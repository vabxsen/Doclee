rules_version = '2';

// No cloud storage of user files exists in this build — images and PDFs are
// processed entirely client-side. Default-deny until a real upload feature
// (and auth strategy) is designed.
service firebase.storage {
  match /b/{bucket}/o {
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
